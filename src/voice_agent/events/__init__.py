"""Event dataclasses + EventBus — internal pub/sub for the voice-agent pipeline.

Originally part of the Textual HUD; kept standalone because the bus is
useful even without a TUI (drives the Rich CLI status panel, agents, etc.).
"""
